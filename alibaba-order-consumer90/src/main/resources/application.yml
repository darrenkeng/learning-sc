server:
  port: 90

spring:
  application:
    name: alibaba-order-consumer
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'